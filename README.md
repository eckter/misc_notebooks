Just to share some jupyter notebooks.

May not be cleaned up.

```
poetry install
poetry run python -m ipykernel install --user --name=notebooks # register the jupyter kernel
poetry run jupyter notebook
```
